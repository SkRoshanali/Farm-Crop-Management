# Database Configuration
DB_URL=jdbc:mysql://127.0.0.1:3306/farm_crop_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=Roshan#099

# JWT Configuration
JWT_SECRET=your_256_bit_secret_key_here_must_be_at_least_32_characters_long
JWT_EXPIRATION=86400000

# Application Profile
SPRING_PROFILES_ACTIVE=dev

# IMPORTANT: 
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. Use strong passwords and secrets in production
# 4. Generate JWT secret using: openssl rand -base64 32
