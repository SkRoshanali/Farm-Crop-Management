# ── Development Profile ──
# SECURITY WARNING: Never commit real credentials! Use environment variables.
spring.datasource.url=${DB_URL:jdbc:mysql://127.0.0.1:3306/farm_crop_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Roshan#099}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Development-specific settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.farmcrop=DEBUG
